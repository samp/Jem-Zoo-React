import React from 'react';
import './Cards-2.css';

const Cards2 = (props) => {
    return (
        <div className="cards-2">
            {props.children}
        </div>
    )
};

export default Cards2;