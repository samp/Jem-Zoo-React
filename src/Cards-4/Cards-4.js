import React from 'react';
import './Cards-4.css';

const Cards4 = (props) => {
    return (
        <div className="cards-4">
            {props.children}
        </div>
    )
};

export default Cards4;